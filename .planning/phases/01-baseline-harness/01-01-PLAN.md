---
phase: "01"
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - config/project.yaml
  - .env.example
  - src/workflow_memory/__init__.py
autonomous: true
requirements: [TASK-01]
must_haves:
  truths:
    - "browser-use==0.12.6 declared as dependency in pyproject.toml"
    - "config/project.yaml defines sqlite_path, artifacts_root, judge_model, optimize_model"
    - "Target site credentials templated in .env.example as MAI_USERNAME / MAI_PASSWORD"
    - "Playwright browser set to chromium"
  artifacts:
    - path: pyproject.toml
      provides: "Project dependencies and CLI entrypoint"
      contains: "browser-use==0.12.6"
    - path: config/project.yaml
      provides: "Runtime configuration for agent, storage, retrieval, and admission"
    - path: .env.example
      provides: "Credential template for MAI site"
  key_links:
    - from: config/project.yaml
      to: src/workflow_memory/config.py
      via: "load_config(path)"
      pattern: "ProjectConfig"
---

<objective>
Select and lock the runtime stack, target site, and benchmark task family for Phase 1.

Decisions:
- Runtime: browser-use 0.12.6 + Python 3.11–3.12 + Playwright/Chromium
- LLM backbone: OpenAI (gpt-4.1-mini judge, gpt-4.1 optimize)
- Target site: MAI university portal (site-specific, credential-gated, stable repeated tasks)
- Persistence: SQLite for run index, JSON files for per-run artifacts
- Config surface: YAML-based ProjectConfig with Pydantic validation

Output: pyproject.toml, config/project.yaml, .env.example, config loader + tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap project with dependencies and config</name>
  <files>pyproject.toml, config/project.yaml, .env.example, src/workflow_memory/config.py, tests/test_config.py</files>
  <action>
Create pyproject.toml with browser-use, playwright, openai, langchain-openai, pydantic, typer,
PyYAML, rapidfuzz, python-dotenv, pytest dependencies.
Create config/project.yaml with all runtime knobs.
Create Pydantic ProjectConfig loader.
Create .env.example with OPENAI_API_KEY, MAI_USERNAME, MAI_PASSWORD.
  </action>
  <done>pip install -e . succeeds; test_config.py passes; config loads without error.</done>
</task>

</tasks>

<success_criteria>
- pyproject.toml declares browser-use==0.12.6
- config/project.yaml parseable by load_config()
- .env.example documents required credentials
- pytest tests/test_config.py passes
</success_criteria>
