---
phase: "02"
plan: "02-01"
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - src/workflow_memory/models.py
  - src/workflow_memory/db.py
  - src/workflow_memory/storage/artifacts.py
  - src/workflow_memory/storage/repository.py
  - tests/test_storage.py
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "RunArtifact Pydantic model captures run_id, site, task_family, run_mode, status, task_input, metrics"
    - "ArtifactStore.write_run_artifacts() writes trace.json, normalized.json, result.json with atomic rollback"
    - "RunRepository.insert_run() persists to SQLite runs table with rollback on DB error"
    - "RunRepository.get_run() round-trips all fields correctly"
    - "Duplicate run_id raises IntegrityError and preserves existing artifacts"
    - "DB schema created by initialize_db() on first connection"
  artifacts:
    - path: src/workflow_memory/models.py
      provides: "RunArtifact, ArtifactPaths, PersistedRunRecord"
    - path: src/workflow_memory/storage/artifacts.py
      provides: "ArtifactStore with atomic write + rollback"
    - path: src/workflow_memory/storage/repository.py
      provides: "RunRepository with SQLite persistence"
    - path: src/workflow_memory/db.py
      provides: "initialize_db() — creates runs table"
  key_links:
    - from: src/workflow_memory/storage/repository.py
      to: src/workflow_memory/db.py
      via: "initialize_db(self.db_path)"
      pattern: "initialize_db"
    - from: tests/test_storage.py
      to: src/workflow_memory/storage/artifacts.py
      via: "ArtifactStore.write_run_artifacts()"
      pattern: "write_run_artifacts"
---

<objective>
Design and implement the memory schema and persistence boundary for Phase 2.

This plan establishes the storage contracts that all later phases depend on:
- RunArtifact: what a run record contains
- ArtifactStore: how raw run files land on disk (atomic, with rollback)
- RunRepository: how run metadata is indexed in SQLite
- The boundary: ArtifactStore owns filesystem paths; RunRepository owns SQL — no cross-inference

Output: models.py, db.py, storage/artifacts.py, storage/repository.py, tests/test_storage.py
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Define run models and DB schema</name>
  <files>src/workflow_memory/models.py, src/workflow_memory/db.py</files>
  <action>
Create RunArtifact Pydantic model, ArtifactPaths TypedDict, PersistedRunRecord TypedDict.
Create initialize_db() that creates the runs table in SQLite.
  </action>
  <done>Models importable; initialize_db() creates runs table without error.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ArtifactStore and RunRepository with full storage contracts</name>
  <files>src/workflow_memory/storage/artifacts.py, src/workflow_memory/storage/repository.py, tests/test_storage.py</files>
  <action>
ArtifactStore.write_run_artifacts(): write trace/normalized/result JSON, rollback directory on error, raise on duplicate.
RunRepository.insert_run(): INSERT to SQLite, rollback artifact_dir on DB error, raise IntegrityError on duplicate.
RunRepository.get_run(): SELECT by run_id, return PersistedRunRecord or None.
Write 5 tests covering happy path, get missing, rollback on DB failure, duplicate insert, failed artifact write rollback.
  </action>
  <done>pytest tests/test_storage.py passes 5/5.</done>
</task>

</tasks>

<success_criteria>
- RunArtifact, ArtifactPaths, PersistedRunRecord defined in models.py
- ArtifactStore writes 3 JSON files atomically with directory rollback on error
- RunRepository persists to SQLite and rolls back artifact_dir on DB error
- Duplicate run_id raises IntegrityError, existing artifacts untouched
- pytest tests/test_storage.py: 5 passed
</success_criteria>
