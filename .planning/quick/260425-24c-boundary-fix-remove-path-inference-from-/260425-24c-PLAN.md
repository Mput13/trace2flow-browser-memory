---
phase: quick
plan: 260425-24c
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workflow_memory/storage/repository.py
  - tests/test_storage.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "RunRepository.insert_run() accepts artifact_dir as an explicit Path parameter and does not call Path(paths['trace']).parent"
    - "On DB error the cleanup shutil.rmtree uses the explicitly passed artifact_dir"
    - "All existing tests in tests/test_storage.py continue to pass"
  artifacts:
    - path: src/workflow_memory/storage/repository.py
      provides: "RunRepository with updated insert_run signature"
      contains: "artifact_dir: Path"
    - path: tests/test_storage.py
      provides: "Updated test callers passing artifact_dir"
  key_links:
    - from: tests/test_storage.py
      to: src/workflow_memory/storage/repository.py
      via: "insert_run(run, paths, artifact_dir=...)"
      pattern: "insert_run.*artifact_dir"
---

<objective>
Remove Path(paths["trace"]).parent filesystem inference from RunRepository.insert_run().

Purpose: The repository layer should not derive filesystem paths — that is the
ArtifactStore's concern. Rollback cleanup should use an artifact_dir passed explicitly
by the caller so the boundary between the storage and DB layers stays clean.

Output: Updated repository.py (new signature) and updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/workflow_memory/storage/repository.py
@src/workflow_memory/storage/artifacts.py
@tests/test_storage.py

<interfaces>
<!-- Relevant signatures before the change. -->

RunRepository.insert_run (current — to be replaced):
```python
def insert_run(self, run: RunArtifact, paths: ArtifactPaths) -> None:
    # ... DB insert ...
    except sqlite3.Error:
        run_dir = Path(paths["trace"]).parent   # <-- inference to remove
        shutil.rmtree(run_dir, ignore_errors=True)
        raise
```

ArtifactStore.write_run_artifacts returns ArtifactPaths and owns run_dir:
```python
run_dir = self.root / "runs" / run.run_id
# ... writes files ...
return ArtifactPaths(trace=str(trace_path), ...)
```

Test that verifies rollback (line 53-84 of tests/test_storage.py):
```python
paths = store.write_run_artifacts(...)
run_dir = Path(paths["trace"]).parent   # already computed separately
repo.insert_run(run, paths)             # caller already knows run_dir
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact_dir parameter to RunRepository.insert_run</name>
  <files>src/workflow_memory/storage/repository.py</files>
  <action>
Change the signature of insert_run from:

  def insert_run(self, run: RunArtifact, paths: ArtifactPaths) -> None:

to:

  def insert_run(self, run: RunArtifact, paths: ArtifactPaths, artifact_dir: Path) -> None:

In the except sqlite3.Error block replace:

  run_dir = Path(paths["trace"]).parent
  shutil.rmtree(run_dir, ignore_errors=True)

with:

  shutil.rmtree(artifact_dir, ignore_errors=True)

The local variable run_dir and the Path import are no longer needed for inference;
keep the Path import only if Path is still referenced elsewhere in the file (it is
used in __init__ for self.db_path, so keep it). Do not change any other method.
  </action>
  <verify>
    <automated>cd /Users/a/MAI/sem2/trace2flow && python -c "import inspect, workflow_memory.storage.repository as r; sig = inspect.signature(r.RunRepository.insert_run); assert 'artifact_dir' in sig.parameters, 'artifact_dir param missing'"</automated>
  </verify>
  <done>insert_run signature contains artifact_dir: Path; no Path(...).parent inference remains in the method body.</done>
</task>

<task type="auto">
  <name>Task 2: Update test callers and run full test suite</name>
  <files>tests/test_storage.py</files>
  <action>
Two call sites in tests/test_storage.py call repo.insert_run(run, paths).
Update each to pass artifact_dir explicitly.

Call site 1 — test_run_repository_and_artifact_store_persist_run (line ~30):
  Before: repo.insert_run(run, paths)
  After:  repo.insert_run(run, paths, artifact_dir=Path(paths["trace"]).parent)

Call site 2 — test_insert_run_cleans_up_artifacts_when_db_insert_fails (line ~78):
  The test already computes run_dir = Path(paths["trace"]).parent on the line above.
  Before: repo.insert_run(run, paths)
  After:  repo.insert_run(run, paths, artifact_dir=run_dir)

No other callers exist outside the test file.
Do not change any assertions or other test logic.
  </action>
  <verify>
    <automated>cd /Users/a/MAI/sem2/trace2flow && python -m pytest tests/test_storage.py -v 2>&1</automated>
  </verify>
  <done>All tests in tests/test_storage.py pass (5 green, 0 failures). No Path(...).parent inference remains in repository.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → repository | artifact_dir Path supplied by caller must point to the run directory that was actually written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24c-01 | Tampering | insert_run artifact_dir arg | accept | artifact_dir is an internal Path from ArtifactStore; no external input reaches it in current scope |
</threat_model>

<verification>
```
cd /Users/a/MAI/sem2/trace2flow && python -m pytest tests/test_storage.py -v
```
All 5 tests must pass. Confirm no `Path(paths["trace"]).parent` remains in repository.py:
```
grep -n 'paths\["trace"\]' src/workflow_memory/storage/repository.py
```
Should return no matches.
</verification>

<success_criteria>
- insert_run(self, run, paths, artifact_dir) is the final signature
- No Path(paths["trace"]).parent line exists anywhere in repository.py
- pytest tests/test_storage.py exits 0 with all tests passing
</success_criteria>

<output>
No SUMMARY file required for quick fixes. Return results inline after execution.
</output>
