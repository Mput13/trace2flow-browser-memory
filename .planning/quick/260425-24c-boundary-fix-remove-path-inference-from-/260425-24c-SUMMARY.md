---
phase: quick
plan: 260425-24c
subsystem: storage
tags: [refactor, boundary, repository]
key-files:
  modified:
    - src/workflow_memory/storage/repository.py
    - tests/test_storage.py
decisions:
  - artifact_dir is now an explicit caller-supplied Path; repository no longer infers filesystem paths from ArtifactPaths
metrics:
  completed: 2026-04-25
---

# Quick 260425-24c: Boundary Fix — Remove Path Inference from RunRepository Summary

**One-liner:** Moved filesystem path derivation out of `RunRepository.insert_run` by adding an explicit `artifact_dir: Path` parameter, keeping the DB and artifact layers cleanly separated.

## Tasks Completed

| Task | Name | Files |
|------|------|-------|
| 1 | Add artifact_dir parameter to RunRepository.insert_run | src/workflow_memory/storage/repository.py |
| 2 | Update test callers and run full test suite | tests/test_storage.py |

## Changes Made

### src/workflow_memory/storage/repository.py

- Changed signature from `insert_run(self, run, paths)` to `insert_run(self, run, paths, artifact_dir: Path)`
- Replaced `run_dir = Path(paths["trace"]).parent; shutil.rmtree(run_dir, ...)` with `shutil.rmtree(artifact_dir, ...)` in the `except sqlite3.Error` block
- `Path` import retained (still used for `self.db_path` in `__init__`)

### tests/test_storage.py

- Call site 1 (`test_run_repository_and_artifact_store_persist_run`, line 30): updated to `repo.insert_run(run, paths, artifact_dir=Path(paths["trace"]).parent)`
- Call site 2 (`test_insert_run_cleans_up_artifacts_when_db_insert_fails`, line 78): updated to `repo.insert_run(run, paths, artifact_dir=run_dir)` (reuses the `run_dir` already computed on the prior line)

## Test Results

```
============================= test session starts ==============================
platform darwin -- Python 3.11.9, pytest-9.0.3
collected 5 items

tests/test_storage.py::test_run_repository_and_artifact_store_persist_run PASSED [ 20%]
tests/test_storage.py::test_run_repository_get_run_returns_none_for_missing_run PASSED [ 40%]
tests/test_storage.py::test_insert_run_cleans_up_artifacts_when_db_insert_fails PASSED [ 60%]
tests/test_storage.py::test_duplicate_insert_keeps_existing_artifacts PASSED [ 80%]
tests/test_storage.py::test_failed_artifact_write_rolls_back_run_directory PASSED [100%]

============================== 5 passed in 0.03s ===============================
```

5 passed, 0 failures.

## Verification

- `artifact_dir` present in `insert_run` signature: confirmed
- `Path(paths["trace"]).parent` inference removed from `repository.py`: confirmed (`grep` returns no match)
- All 5 tests green: confirmed

## Deviations from Plan

None — plan executed exactly as written.
